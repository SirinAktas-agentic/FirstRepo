# Bağımlılıklar (uygulamanın harici bağımlılığı yoktur; yalnızca test araçları buraya kurulabilir)
node_modules/

# Eski Expo derleme çıktıları (artık kullanılmıyor; yanlışlıkla üretilirse depoya girmesin)
dist/
.expo/
web-build/

# Ortam / editör
.env
.env*.local
.DS_Store
*.pem

# debug
npm-debug.*
yarn-debug.*
yarn-error.*
